# 拍照识词导出助手 - 后端依赖
# Python 3.9+

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# OCR 引擎 - PaddleOCR
# 安装前请确认已安装 PaddlePaddle:
#   CPU: pip install paddlepaddle
#   GPU: pip install paddlepaddle-gpu
paddleocr>=2.7.0

# NLP 处理
spacy>=3.7.0
nltk>=3.8.0

# 英语语言模型（spaCy）
# 安装后需手动下载模型:
#   python -m spacy download en_core_web_sm

# HTTP 客户端（可选，用于测试）
httpx>=0.27.0